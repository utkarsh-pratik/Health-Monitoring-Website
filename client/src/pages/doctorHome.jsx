import React from 'react';

const DoctorHome = () => {
  return (
    <div 
      style={{
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        background: '#4c51bf', // a nice blue-purple gradient base
        color: 'white',
        fontSize: '2rem',
        fontWeight: 'bold',
        fontFamily: 'Arial, sans-serif',
      }}
    >
      this is doctor home
    </div>
  );
};

export default DoctorHome;
